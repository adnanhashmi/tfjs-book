// The runScript method code
function runScript() {

    document.getElementById("message").innerHTML = country.name + " was founded " + country.ageInYears + " years ago, and is presently ruled by " + country.rulingParty + ".";
 }
 